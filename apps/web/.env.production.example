# Advertix Web â€” production env template
# -----------------------------------------------------------------------------
# Copy into Vercel â†’ Project â†’ Settings â†’ Environment Variables.
# NEVER commit real secrets here. See docs/DEPLOY.md for where to obtain each.

# === Clerk (Production instance) ===
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_live_REPLACE_ME
CLERK_SECRET_KEY=sk_live_REPLACE_ME

# === Backend URL ===
# The browser hits this directly. Must include https:// and NO trailing slash.
NEXT_PUBLIC_API_URL=https://your-api.railway.app

# === Anthropic Claude ===
# Used by /apps/web/app/api/ai/* server-side proxy routes. Server-only â€” do
# NOT prefix with NEXT_PUBLIC_.
ANTHROPIC_API_KEY=sk-ant-REPLACE_ME
